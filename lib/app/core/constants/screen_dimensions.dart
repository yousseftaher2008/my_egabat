import 'package:flutter/material.dart';
import 'package:get/get.dart';

Size pageDimensions() => Get.size;
double pageHeight() => pageDimensions().height;
double pageWidth() => pageDimensions().width;
